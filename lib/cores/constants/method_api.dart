// enum MethodApi { get, post, put, delete, update }
// enum ServiceStatus { loading, completed, error, warning, cancel }
enum MethodApi { get, post, put, delete, patch }

enum ServiceStatus { loading, completed, error, warning, cancel }
